[
  {
    "id": "async-provider-retry",
    "fault": "The first asynchronous model request returns a transient server error.",
    "expected": "The provider retries without blocking the event loop and returns the second successful response.",
    "test_node": "tests/test_async_model_provider.py::test_async_provider_retries_recoverable_http_error"
  },
  {
    "id": "nonrecoverable-no-retry",
    "fault": "The asynchronous model provider returns a non-recoverable HTTP response.",
    "expected": "The provider exposes a safe error and does not retry the request.",
    "test_node": "tests/test_async_model_provider.py::test_async_provider_does_not_retry_nonrecoverable_error"
  },
  {
    "id": "async-tool-timeout",
    "fault": "An asynchronous Tool exceeds its registered timeout.",
    "expected": "The registry cancels the wait and returns a recoverable error Observation.",
    "test_node": "tests/test_tool_registry_async.py::test_execute_async_timeout_returns_recoverable_error"
  },
  {
    "id": "worker-permission-error",
    "fault": "A Worker asks for a Tool outside its code allowlist.",
    "expected": "The Worker receives a permission error and cannot escape its isolation boundary.",
    "test_node": "tests/test_worker_agent.py::test_worker_permission_error_blocks_tool_outside_allowlist"
  },
  {
    "id": "main-react-model-fallback",
    "fault": "The Main ReAct decision model remains unavailable after retries.",
    "expected": "The production graph returns an explicit provider-limitation draft instead of pretending the teacher request was unclear.",
    "test_node": "tests/test_main_react_graph.py::test_main_react_model_error_returns_provider_limitation_draft"
  },
  {
    "id": "unregistered-tool-feedback",
    "fault": "Main requests a Tool name that is not registered.",
    "expected": "Code rejects the call, records a recoverable Observation, and Main can finish safely.",
    "test_node": "tests/test_main_react_graph.py::test_unregistered_tool_error_becomes_feedback_then_main_can_recover"
  },
  {
    "id": "main-react-step-budget-stop",
    "fault": "Main ReAct consumes its maximum number of turns.",
    "expected": "The production graph returns a bounded limitation draft instead of looping.",
    "test_node": "tests/test_main_react_graph.py::test_main_react_step_budget_returns_bounded_fallback"
  },
  {
    "id": "safe-error-metadata",
    "fault": "An internal model error contains a private response body and network details.",
    "expected": "Only the safe code, retry counters, status, and recoverability are exposed.",
    "test_node": "tests/test_model_errors.py::test_model_provider_safe_metadata_excludes_provider_payloads"
  },
  {
    "id": "async-session-busy-error",
    "fault": "A teacher submits another request while the same session already has an active run.",
    "expected": "The asynchronous API rejects the conflicting request with a session-busy error.",
    "test_node": "tests/test_async_api.py::test_same_session_busy_error_rejects_a_second_active_run"
  },
  {
    "id": "parallel-worker-partial-error",
    "fault": "One Worker branch fails while an independent sibling succeeds.",
    "expected": "The merge preserves the successful Observation and exposes the failed branch as recoverable evidence.",
    "test_node": "tests/test_main_react_graph.py::test_parallel_worker_error_preserves_successful_sibling"
  },
  {
    "id": "user-prompt-injection-boundary",
    "fault": "A user asks Main to ignore its rules and reveal the system prompt.",
    "expected": "The deterministic request guard blocks the request before the model-backed Main agent runs.",
    "test_node": "tests/test_main_react_graph.py::test_request_guard_safely_blocks_prompt_injection_before_main_model_call"
  },
  {
    "id": "retrieved-prompt-injection-boundary",
    "fault": "A Tool observation contains instruction-like text asking Main to reveal its prompt.",
    "expected": "The prompt projection removes the instruction-like text while preserving safe evidence fields.",
    "test_node": "tests/test_main_react_agent.py::test_main_agent_safely_removes_instruction_like_text_from_observations"
  }
]
